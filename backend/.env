PORT=3000
HOST=0.0.0.0

# Nodemailer (Gmail SMTP example) - replace with your credentials or use SendGrid SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASS=your-email-password-or-app-password

# From address shown in verification emails
EMAIL_FROM="Transport Tracker" <your.email@gmail.com>

# Optional AI microservice address (if running)
AI_URL=http://localhost:5000/predict
